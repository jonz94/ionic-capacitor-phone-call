import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.jonz94.app.phonecalldemo',
  appName: 'Phone Call Demo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
